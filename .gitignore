# machine-local additions — not committed
local.star

# Claude Code local tool-permission cache
.claude/settings.local.json

# resolved dependency lockfile — regenerated by meowctl sync
deps.lock

# runtime state files generated by meowctl
rollback.jsonl
state.toml

# user-level module config — dotmeow is a registry module now
deps.mod
