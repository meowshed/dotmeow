# ~/.config/fish/config.fish — managed by dotmeow
# https://github.com/meowshed/dotmeow

# --- meowctl shell integration ---
# Activates all installed components that emit shell hooks (mise, starship,
# atuin, zoxide, direnv, meowvim, git-config, etc.)
meowctl shell fish | source

